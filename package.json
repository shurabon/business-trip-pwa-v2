{
  "name": "business-trip-pwa",
  "version": "1.0.0",
  "description": "Offline-First PWA for Business Trips & Expenses",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 3001",
    "build": "vite build",
    "preview": "vite preview --port 3001",
    "deploy": "npm run build && npx -y gh-pages -d dist"
  },
  "dependencies": {
    "dexie": "^4.0.10",
    "docx": "^9.7.1",
    "exceljs": "^4.4.0",
    "html2canvas": "^1.4.1",
    "jspdf": "^2.5.2"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
